-- do not use this. use lazygit or neogit instead

return {
    "tpope/vim-fugitive", -- Fugitive is the premier Vim plugin for Git. Or maybe it's the premier Git plugin for Vim?
    dependencies = {
      "lewis6991/gitsigns.nvim",
    }
  }
